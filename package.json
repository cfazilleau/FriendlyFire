{
  "name": "friendlyfire",
  "version": "1.0.0",
  "description": "A Typescript modular discord bot",
  "main": "main.js",
  "scripts": {
    "clean": "rm -rf build",
    "build": "rm -rf build && tsc",
    "start": "rm -rf build && tsc && cp -r plugins/ build/plugins/ && node build/main.js"
  },
  "author": "faz.cle",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/cfazilleau/friendlyfire.git"
  },
  "devDependencies": {
    "@types/node": "^17.0.29",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@discordjs/builders": "^0.13.0",
    "@discordjs/rest": "^0.4.1",
    "discord-api-types": "^0.32.0",
    "discord.js": "^13.6.0",
    "dotenv": "^16.0.0"
  }
}
